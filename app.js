const API='https://api.github.com',OWNER='Aniket12-coder07',S={token:localStorage.getItem('gh_token')||'',user:null,files:[{name:'main',ext:'py',code:''}],active:0};const $=s=>document.querySelector(s);async function gh(p,o={}){const r=await fetch(API+p,{...o,headers:{Authorization:`token ${S.token}`,Accept:'application/vnd.github.v3+json',...(o.body?{'Content-Type':'application/json'}:{})}});const d=await r.json().catch(()=>({}));if(!r.ok)throw new Error(d.message||'API error');return d;}async function deploy(){const btn=$('.btn-main');try{btn.disabled=true;const repoName=$('#repoName').value;for(const f of S.files){await gh(`/repos/${OWNER}/${repoName}/contents/${f.name}.${f.ext}`,{method:'PUT',body:JSON.stringify({message:`Add ${f.name}`,content:btoa(f.code)})});}alert('Deployed!');}catch(e){alert(e.message);}finally{btn.disabled=false;}}window.onload=()=>{if(S.token)$('.btn-main').onclick=deploy;};
